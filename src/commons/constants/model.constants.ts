/**
 * all the constants used in the models are place here
 */

export const MessageTypes = ['text', 'image'];

// display media types used in Task model
export const TaskDisplayMediaTypes = ['image', 'video'];

// task progress types
export const TaskProgressTypes = ['planned', 'in-progress', 'complete', 'quit'];
